package com.konsol.beatstream.domain.enumeration;

/**
 * The DownloadStatus enumeration.
 */
public enum DownloadStatus {
    CONVERSION,
    PENDING,
    SCHEDULED,
    IN_PROGRESS,
    COMPLETED,
    FAILED,
    PAUSED,
}
